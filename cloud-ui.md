# deve ser capaz de fazer

1 - Desenvolver o controle de estado através do UseState e UseEffect.
2 - Controlar o render através de componentes e rotas.
3 - Controlar os eventos através de mudanças de componentes e dados.
4 - Implementar validação e autenticação de usuários através de rotas/API's externas.

# se possivel implemente estes requisitos:

    - Arquitetura flux;
    - Redux;
    - Autenticação com redux;
    - Noção de backend com strapi.

# coisa que precisam constar no projeto

- Desenvolver o controle de estado na aplicação com React. UseState,UseEffect, mudar status através do Set do hook
- passar os parâmetros para os componentes
- verificação de render nos parâmetros
- mudança de estado no UseState para renderizar o componente
- mudança de estado no UsEffect para renderizar o componente
- controle de eventos através da mudança de componentes e de dados
- criar componente que escuta a mudança do parâmetro para alterar o estado atual
- criar função que mude o estado do elemento
- criar função que mude o estado do componente
- criar um serviço de autenticação
- devida mudança no middleware para verificar a autenticação
- mudar o interceptor para verificar a autenticação do usuário
